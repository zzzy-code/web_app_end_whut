<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="server.app_end.repository.ArticleRepository">

    <!-- 添加文章 -->
    <insert id="add" parameterType="server.app_end.pojo.Article" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article (title, content, name)
        VALUES (#{title}, #{content}, #{name})
    </insert>

    <!-- 修改文章 -->
    <update id="edit" parameterType="server.app_end.pojo.Article">
        UPDATE article
        SET title = #{title}, content = #{content}
        WHERE id = #{id}
    </update>

    <!-- 删除文章 -->
    <delete id="deleteById" parameterType="int">
        DELETE FROM article WHERE id = #{id}
    </delete>

    <!-- 查询文章列表 -->
    <select id="list" resultType="server.app_end.pojo.Article">
        SELECT id, title, content, name
        FROM article
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                OR content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="authorName != null and authorName != ''">
                AND name = #{authorName}
            </if>
        </where>
        ORDER BY id ASC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 查询总数量 -->
    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM article
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                OR content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="authorName != null and authorName != ''">
                AND name = #{authorName}
            </if>
        </where>
    </select>

    <!-- 查询单个文章 -->
    <select id="findById" resultType="server.app_end.pojo.Article">
        SELECT id, title, content, name
        FROM article
        WHERE id = #{id}
    </select>

</mapper>
